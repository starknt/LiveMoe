import { Typography, styled } from '@mui/material';

const TinyText = styled(Typography)({
  fontSize: '0.85rem',
  opacity: 0.45,
  fontWeight: 500,
  letterSpacing: 0.2,
});

export default TinyText;
